package com.example.mynoteapp.ui.theme

import androidx.compose.ui.graphics.Color

// === SHARED COLORS (used in both themes) ===
val PrimaryColor    = Color(0xFF104C64) // Deep teal blue
val SecondaryColor  = Color(0xFFD59D80) // Warm peach
val TertiaryColor   = Color(0xFFC0754D) // Burnt orange
val ErrorColor      = Color(0xFFB6410F) // Reddish-orange

// === LIGHT THEME COLORS ===
val LightBackground = Color(0xFFFFFFFF) // Plain white
val LightSurface    = Color(0xFFC6C6D0) // Soft gray
val OnPrimaryLight  = Color.White
val OnBackgroundLight = Color.Black
val OnSurfaceLight  = Color.Black
val OnErrorLight    = Color.White

// === DARK THEME COLORS ===
val DarkBackground  = Color(0xFF0D1D25) // Very dark cyan-black
val DarkSurface     = Color(0xFF104C64) // Slightly lighter than background
val OnPrimaryDark   = Color.White
val OnBackgroundDark = Color.White
val OnSurfaceDark   = Color.White
val OnErrorDark     = Color.White
